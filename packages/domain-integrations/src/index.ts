// @verocrest/domain-integrations — client-safe surface (types only). Server data
// access is in './server'; Server Actions in './actions'.
export {
  GOOGLE_PROVIDER,
  type ConnectionStatus,
  type GoogleConnectionView,
} from './connection/types';

{
  "name": "@verocrest/domain-scoring",
  "version": "0.1.0",
  "private": true,
  "description": "Lead Intelligence scoring (docs/04 §5.3–5.5). Sprint 4.9: deterministic fit = ICP match + website intelligence (renormalized over available components), score-lead explainability, LIE write-locked lead_scores + score versioning. Readiness/opportunity, enrichment, and the outreach queue remain the full Sprint 7.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./actions": "./src/actions.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@verocrest/platform-ai-router": "workspace:*",
    "@verocrest/platform-event-bus": "workspace:*",
    "@verocrest/platform-integrations": "workspace:*",
    "@verocrest/platform-tenancy": "workspace:*"
  },
  "peerDependencies": {
    "next": "^15.0.0"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.48.1",
    "@types/node": "catalog:",
    "next": "^15.1.6",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
